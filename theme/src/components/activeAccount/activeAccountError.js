import React from 'react';
import { text } from '../../lib/settings';

const ActiveAccountError = () => {
	return <div className="has-text-centered">{text.activeAccountError}</div>;
};

export default ActiveAccountError;
