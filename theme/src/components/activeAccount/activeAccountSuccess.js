import React from 'react';
import { text } from '../../lib/settings';

const ActiveAccountSuccess = () => {
	return <div className="has-text-centered">{text.activeAccountSuccess}</div>;
};

export default ActiveAccountSuccess;
