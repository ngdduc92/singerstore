import React from 'react';
import { text } from '../../lib/settings';

const ResetPasswordSuccess = () => {
	return <div className="has-text-centered">{text.resetPasswordSuccess}</div>;
};

export default ResetPasswordSuccess;
